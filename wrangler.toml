name = "heimsauna-klar"
compatibility_date = "2026-08-21"

# Static-assets-only Worker: there is intentionally no `main` entry point and
# no OpenNext runtime bundle.
[assets]
directory = "./out"
not_found_handling = "404-page"
html_handling = "auto-trailing-slash"
